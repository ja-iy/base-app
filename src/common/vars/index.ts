import { APP_THEME } from "./theme"

// app global variables // - initialze in /src/client/styles/vars.sass if neccessary

const MOBILE_WIDTH = 900

const SITE_TITLE = 'Base Template'

export { 
    MOBILE_WIDTH, 
    SITE_TITLE, 
    APP_THEME, 

}